node_modules/
*.log
*.db
.DS_Store
docs/superpowers/

# Personal config (landing page patterns, etc.) — never push to GitHub
extension/config.local.js

# Claude<->Codex bridge (control-plane artifacts, not part of the extension)
.bridge/
